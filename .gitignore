# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/
*.egg

# venv / package manager
.venv/
.uv-cache/
uv.lock.bak

# Tests / Coverage
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml

# Type checkers / Linter
.mypy_cache/
.ruff_cache/

# IDE
.vscode/
.idea/
*.swp

# Claude Code (lokal akkumulierte Session-Allowlist, user-spezifisch)
.claude/

# OS
Thumbs.db
.DS_Store

# Installer-Bundle (Embedded-Python + Dependencies, ca. 100 MB).
# Wird via installer\bundle-python.ps1 frisch erzeugt — nicht commiten.
installer/bundle/python/
installer/out/

# OPN-Cockpit runtime data (Sicherheits-Schutz)
# Auch wenn devices.json absichtlich Klartext-Struktur hat (R-NFR-5),
# wird Laufzeit-Datenverzeichnis nie ins Repo geschoben.
appdata/
local-runtime/
*.jsonl
audit.log
audit.jsonl
settings.local.json
